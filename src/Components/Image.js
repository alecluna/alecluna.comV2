import React from "react";

const Image = (...props) => {
  return (
    <div>
      {/* <img src={require(`../images/${props.image.toLowerCase()}.jpeg`)} /> */}
    </div>
  );
};

export default Image;
